#include "main.h"

/**
 * _abs - computes the abs value of an interger n
 * @n: is the interger in the question
 *
 * Return: abs of intergers
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}

